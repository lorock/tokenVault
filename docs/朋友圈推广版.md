你的 2FA 验证码密钥，可能正被悄悄同步到别人服务器。

我做了一個小工具——令牌盒。

一句话：密钥从不出设备。本地加密（AES-256-GCM），不上传任何服务器，连我自己都看不到你存了啥。断网也能出码，浏览器打开就用，不用装 App。

那些"方便多端同步"的验证器，等于把家门钥匙复印一份寄存别人家——服务器一拖库，密钥跟着泄露。

微信里点不了外链，两种方式给你：

① 长按复制这个域名，粘到浏览器打开：
**k.sre.wang**

② 直接扫下面二维码

（配图：发布时上传 `qrcode-k-sre-wang.png` 这张二维码）

做它不为秀技术。是有次同事问"密钥会不会被同步到他们服务器"，发现大多数产品答不上来。于是做了个不给任何人留后门的版本。

在意自己 2FA 密钥到底存在哪的，试试看。
